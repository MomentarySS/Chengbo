# 澄波隐私说明

澄波是本地运行的广播收听应用。核心承诺：**直播在线播放；播客可按需下载。不收集收听内容。** 设置页「隐私说明」与本文同一套文案。

## 音频

- 直播电台只在内存中缓冲，不会保存成录音文件。
- 播客默认在线播放，并记住进度（毫秒）。
- 你主动下载的播客单集会保存在应用私有目录，可在设置中查看占用并清除；删除播客时也会去掉对应下载。

## 图片与本机数据

- 电台台标可能缓存到设备临时目录，占用少量磁盘；可在设置中查看并清除。
- 收藏、最近播放、上次收听、手动添加的电台、本机更换的精选/发现台流地址、**收听范围**（类型 / 省份选择）、分类和主题偏好只存在本机（SharedPreferences），不上传。

## 网络请求

- 播放时向你选择的直播源 / RSS 地址请求音视频流。
- 首次启动须先选择收听范围（类型 / 省份 / 全部精选），确认后才会检测直播源是否可访问，并读取少量响应内容确认是播放列表或音频（不保存）。之后只在电台页下拉刷新或设置「电台管理 → 检测可播放的源」时重测；修改「收听范围」也会重新检测。平时打开和「刷新电台列表」不再测。能播的电台 id、你隐藏的电台 id 只记在本机。本机一直缓冲或播失败的台会按隐藏处理。
- 若开启「Radio Browser 发现」，会向 Radio Browser 公开目录请求中国大陆、中文语言和港澳台电台（请求带应用 User-Agent `Chengbo/1.5.0`）。港澳台默认不显示。关闭开关后不再请求该目录。
- 会读取系统代理（Internet 选项或 `HTTPS_PROXY`），并检测本机 `127.0.0.1` 上 Clash / NekoBox 等常见端口，用来拉 RSS 和目录。不上传代理密码或浏览记录。
- 若使用设置里的 Podcast Index 搜索，会向其公开目录查询节目元数据和 RSS 地址。API 密钥只存在本机。不去搜索则不会请求。
- 「新一集通知」默认关闭。打开后才会按至少 6 小时的间隔请求你已订阅的 RSS；第一次只记下最新一集，之后有新 guid 才通知。
- 仅 Android：你点「投到电视」后，会把当前流地址送到同一网络上的 Chromecast。不点则不连接 Google Cast。
- 应用不收集、不上报收听内容或个人身份。

## 权限（Android）

- 网络：播放与刷新列表。
- 通知（Android 13+）：后台播放时显示媒体通知栏；打开「新一集通知」后也会用来提醒订阅更新。可在系统设置中关闭；关闭后后台播放可能被系统限制。
- 加速度计（可选）：仅在打开「摇一摇延长睡眠」且睡眠定时运行时读取，用于再加 5 分钟。数据不离开本机。
- 桌面小组件：只在本机桌面显示当前台名/单集名和播放状态，不上传。
