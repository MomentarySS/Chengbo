## 改动计划

共 3 个文件，4 处修改：

### 1. `lib/shared/widgets/now_playing_top_bar.dart`
- 移除 `title` 参数（`required` → 去掉）
- 去掉 `Expanded → Center → Text(title)` 标题 widget
- 保留左侧返回按钮和右侧投屏按钮，Row 直接由两个固定宽度 widget 组成

### 2. `lib/shared/widgets/podcast_now_playing.dart`
- `NowPlayingTopBar(title: '播客')` → `NowPlayingTopBar()`
- `SizedBox(height: 4)`（TopBar 下间距）→ `SizedBox(height: 8)`，与电台统一
- `_EpisodeHeader`：`CrossAxisAlignment.start` → `CrossAxisAlignment.center`，标题和播客名居中

### 3. `lib/shared/widgets/radio_now_playing.dart`
- `NowPlayingTopBar(title: '电台')` → `NowPlayingTopBar()`
- `SizedBox(height: 14)`（TopBar 下间距）→ `SizedBox(height: 8)`，与播客统一
- `_StationHeader`：`CrossAxisAlignment.start` → `CrossAxisAlignment.center`，电台名和副标题居中

### 验证
- `flutter analyze` 无警告
- 现有 74 个测试全绿（布局改动不影响纯逻辑测试）