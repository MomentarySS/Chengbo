两处硬编码的"265"文本需要更新为"378"：

1. **`lib/core/station/station_catalog_selection.dart:162`**  
   `'加载全部国内精选电台（约 265 台）'` → `'加载全部国内精选电台（约 378 台）'`  
   （`StationCatalogSelection.detail()` 方法，当 `allCurated` 为 true 时返回的描述文本，在设置页的「收听范围」摘要里显示）

2. **`lib/features/radio/station_catalog_setup_screen.dart:121`**  
   `'约 265 台，首次检测较慢'` → `'约 378 台，首次检测较慢'`  
   （首次启动引导页和设置「收听范围」里「加载全部精选」开关的副标题）

改动后无需重建 APK 再发版——下次 `flutter build` 时自动带上。如果用户想立即生效，可以跑 `flutter build windows --release` 或 `scripts/pack.ps1` 重新打包。